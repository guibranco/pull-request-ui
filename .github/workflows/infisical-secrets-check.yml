name: Infisical secrets check

on:
  workflow_dispatch:
  pull_request:

jobs:
  secrets-scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Infisical secrets check
        uses: guibranco/github-infisical-secrets-check-action@003cf722c5de227b24f6f9ee24a4bdd6f0832ec9 # v5.2.5
