name: Infisical secrets check

on:
  workflow_dispatch:
  pull_request:

jobs:
  secrets-scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Infisical secrets check
        uses: guibranco/github-infisical-secrets-check-action@1623c839320cd476be190e4fc82d2ec8f565c060 # v5.2.8
